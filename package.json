{
  "name": "clawkeys",
  "version": "0.1.0",
  "description": "TypeScript OpenCode + CH57x control CLI backed by Node-API binaries",
  "type": "module",
  "bin": {
    "ck": "./bin/ck.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "bin",
    "LICENSE",
    "README.md",
    "clawkeys.png"
  ],
  "scripts": {
    "build": "npm run build:ts && npm run build:napi",
    "build:ts": "tsc",
    "build:napi": "napi build --release --platform --js-package-name clawkeys --manifest-path ./crates/native/Cargo.toml",
    "lint": "npm run build:ts -- --pretty false",
    "test": "npm run build:ts && node --test",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "clawkeys-darwin-arm64": "0.1.0",
    "clawkeys-darwin-x64": "0.1.0",
    "clawkeys-linux-x64-gnu": "0.1.0",
    "clawkeys-linux-arm64-gnu": "0.1.0",
    "clawkeys-linux-x64-musl": "0.1.0",
    "clawkeys-win32-x64-msvc": "0.1.0"
  },
  "napi": {
    "binaryName": "clawkeys"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.8.3"
  },
  "license": "MIT"
}
